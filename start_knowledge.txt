push 전에 pull
<pull>
-git pull origin master //해당 폴더로 repository에서 끌어옴

<push>
-git status  //현재 로컬폴더와 git과의 상태 확인
 
-git add . //변경된 파일을 tracking

-git commit -m "message" //인식할 수 있는  commit 달아

-git push origin +master 

